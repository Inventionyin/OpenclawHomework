# 最终演示验收

## 1. 已验证的完整链路

```text
飞书消息
  -> https://openclaw.evanshine.me/webhook/feishu
  -> OpenClaw 解析自然语言
  -> GitHub Actions
  -> 启动电商前端
  -> 执行 UI 自动化
  -> 飞书卡片返回结果和报告入口
```

## 2. 推荐演示话术

在飞书里依次发送：

```text
你好
```

```text
帮助
```

```text
绑定我
```

```text
帮我跑一下 main 分支的 UI 自动化冒烟测试
```

## 3. GitHub Actions 成功截图

![GitHub Actions 成功记录](assets/github-actions-success.png)

成功记录：

```text
https://github.com/Inventionyin/OpenclawHomework/actions/runs/25098166593
```

## 4. 飞书结果卡片效果

下面这张图按当前飞书 interactive card 字段生成，用来展示最终返回到飞书里的卡片形态：

![飞书结果卡片效果](assets/feishu-result-card-demo.png)

飞书里实际可演示的消息：

- `你好`：机器人回复自己是 OpenClaw UI 自动化助手
- `帮助`：机器人回复可用指令
- `绑定我`：把触发权限限制到当前飞书用户
- `帮我跑一下 main 分支的 UI 自动化冒烟测试`：触发 GitHub Actions，并在结束后返回结果卡片

线上服务已启用绑定要求：未发送 `绑定我` 前，测试触发请求会被拒绝；绑定成功后，其他飞书用户不能覆盖这个绑定。

## 5. 作业要求第 5 项如何完成

老师要求：

```text
对飞书回复和 GitHub Actions 成功页面截图，保存到最终演示文档
```

这个仓库里对应的最终演示文档就是：

```text
docs/最终演示验收.md
```

你只需要准备两张真实截图，然后替换当前文档里的示意图即可：

1. `docs/assets/github-actions-success.png`
   - 内容：GitHub Actions 成功页面
   - 推荐截图范围：workflow 名称、绿色成功状态、run 编号、执行时间

2. `docs/assets/feishu-result-card-demo.png`
   - 内容：飞书里的真实回复
   - 推荐截图范围：你发送“帮我跑一下 main 分支的 UI 自动化冒烟测试”后的
     - 收到提示
     - 最终结果卡片
     - GitHub Actions / Allure 链接

建议操作顺序：

```text
1. 飞书发送：绑定我
2. 飞书发送：帮我跑一下 main 分支的 UI 自动化冒烟测试
3. 截图飞书里的最终回复
4. 打开 GitHub Actions 对应 run 页面并截图
5. 把两张图分别覆盖到 docs/assets/ 目录下同名文件
6. 提交最终文档
```

如果只是为了快速交作业，也可以先保留当前仓库里的示意图；如果老师要求“真实运行截图”，就把上面两张图替换成你自己本次演示时截到的版本。

## 6. 当前加分功能

- 飞书消息卡片结果通知
- Allure 报告入口
- 普通聊天和帮助回复
- OpenClaw 自然语言解析
- GitHub Actions 状态轮询
- `绑定我` 用户权限限制
- GitHub Actions 自动启动电商前端并执行 UI 自动化
