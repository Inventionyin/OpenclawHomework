# 云服务器接手说明

## 1. 服务器用途

这台服务器用于部署 OpenClawHomework 的飞书 Webhook 桥梁服务。

当前链路：

```text
飞书 / 模拟 Webhook
  -> https://openclaw.evanshine.me/webhook/feishu
  -> Nginx
  -> Node 桥梁服务 127.0.0.1:8788
  -> 可选：OpenClaw 解析自然语言指令
  -> GitHub API workflow_dispatch
  -> GitHub Actions 执行 UItest
```

## 2. 服务器基本信息

```text
系统：Debian GNU/Linux 13
公网 IP：38.76.178.91
域名：openclaw.evanshine.me
SSH 用户：root
SSH 端口：22
```

注意：

- 不要把服务器密码、GitHub Token、飞书 App Secret 写进 GitHub 仓库。
- GitHub Token 当前存放在服务器 `/etc/openclaw-feishu-bridge.env`，文件权限为 `600`。

## 3. 已安装组件

```text
Git
Nginx
Node.js
NPM
systemd service
```

当前验证过的版本：

```bash
node -v
npm -v
nginx -v
git --version
```

## 4. 项目部署位置

```text
/opt/OpenclawHomework
```

查看当前代码版本：

```bash
cd /opt/OpenclawHomework
git log --oneline -5
```

更新代码：

```bash
cd /opt/OpenclawHomework
git fetch origin main
git reset --hard origin/main
systemctl restart openclaw-feishu-bridge
```

## 5. 服务信息

systemd 服务名：

```text
openclaw-feishu-bridge
```

常用命令：

```bash
systemctl status openclaw-feishu-bridge --no-pager -l
systemctl restart openclaw-feishu-bridge
systemctl stop openclaw-feishu-bridge
systemctl start openclaw-feishu-bridge
journalctl -u openclaw-feishu-bridge -n 100 --no-pager
```

服务监听：

```text
Node 桥梁服务：127.0.0.1:8788
Nginx 对外入口：0.0.0.0:80
Nginx HTTPS：0.0.0.0:443
```

为什么不是 8787：

```text
服务器上已有一个 Python 服务占用了 127.0.0.1:8787。
因此 OpenClawHomework 桥梁服务改用 8788。
```

## 6. 环境变量文件

位置：

```text
/etc/openclaw-feishu-bridge.env
```

里面包含：

```text
PORT=8788
GITHUB_OWNER=Inventionyin
GITHUB_REPO=OpenclawHomework
GITHUB_WORKFLOW_ID=ui-tests.yml
GITHUB_REF_NAME=main
UI_TEST_RUN_MODE=contracts
UI_TEST_TARGET_REPOSITORY=Inventionyin/UItest
UI_TEST_TARGET_REF=main
UI_TEST_BASE_URL=http://127.0.0.1:5173
OPENCLAW_PARSE_ENABLED=true
OPENCLAW_MODEL=xfyun/astron-code-latest
FEISHU_RESULT_NOTIFY_ENABLED=false
FEISHU_APP_ID=...
FEISHU_APP_SECRET=...
GITHUB_TOKEN=...
```

修改后需要重启：

```bash
systemctl restart openclaw-feishu-bridge
```

说明：

- `OPENCLAW_PARSE_ENABLED=false` 时，只支持固定格式指令，例如 `/run-ui-test main contracts`。
- `OPENCLAW_PARSE_ENABLED=true` 时，会先尝试固定格式；固定格式不匹配时，再调用 OpenClaw 把自然语言解析成测试参数。
- 当前云服务器已经安装 OpenClaw，并已验证自然语言解析可用。
- `FEISHU_RESULT_NOTIFY_ENABLED=true` 且配置飞书 App ID / App Secret 后，服务会轮询 GitHub Actions 并主动发送最终结果到飞书。

## 7. Nginx 配置

配置文件：

```text
/etc/nginx/sites-available/openclaw-feishu-bridge
```

启用链接：

```text
/etc/nginx/sites-enabled/openclaw-feishu-bridge
```

当前代理关系：

```text
https://openclaw.evanshine.me/*
  -> http://127.0.0.1:8788/*
```

HTTP 会被 Certbot/Nginx 重定向到 HTTPS。

检查并重载：

```bash
nginx -t
systemctl reload nginx
```

## 8. 健康检查

服务器内检查：

```bash
curl http://127.0.0.1:8788/health
curl http://127.0.0.1/health
```

外部检查：

```powershell
Invoke-RestMethod -Uri "https://openclaw.evanshine.me/health"
```

期望返回：

```json
{"ok":true}
```

## 9. 模拟飞书请求

PowerShell 示例：

```powershell
$payload = @{
  event = @{
    sender = @{
      sender_id = @{
        open_id = "server-public-test"
      }
    }
    message = @{
      content = '{"text":"/run-ui-test main contracts"}'
    }
  }
} | ConvertTo-Json -Depth 10

Invoke-RestMethod `
  -Method Post `
  -Uri "https://openclaw.evanshine.me/webhook/feishu" `
  -ContentType "application/json" `
  -Body $payload
```

成功返回：

```json
{
  "ok": true,
  "message": "UI 自动化测试已触发：分支 main，模式 contracts",
  "actionsUrl": "https://github.com/Inventionyin/OpenclawHomework/actions/workflows/ui-tests.yml",
  "workflowRunUrl": "https://github.com/Inventionyin/OpenclawHomework/actions/runs/...",
  "targetRepository": "Inventionyin/UItest",
  "targetRef": "main",
  "runMode": "contracts"
}
```

## 10. 已验证结果

公网接口已经验证通过：

```text
GET  https://openclaw.evanshine.me/health
POST https://openclaw.evanshine.me/webhook/feishu
```

公网 webhook 触发的 GitHub Actions 已成功：

```text
https://github.com/Inventionyin/OpenclawHomework/actions/runs/25092747238
https://github.com/Inventionyin/OpenclawHomework/actions/runs/25093200188
https://github.com/Inventionyin/OpenclawHomework/actions/runs/25098166593
```

云服务器已经验证 OpenClaw 自然语言解析：

```text
输入：帮我跑一下 main 分支的 UI 自动化冒烟测试
解析结果：targetRef=main, runMode=smoke
最终结果：GitHub Actions success
```

这条验证走的是公网 HTTPS：

```text
POST https://openclaw.evanshine.me/webhook/feishu
```

## 11. 域名和 HTTPS

域名和 HTTPS 已配置完成。

DNS：

```text
openclaw.evanshine.me -> 38.76.178.91
```

证书：

```text
Let's Encrypt
证书路径：/etc/letsencrypt/live/openclaw.evanshine.me/fullchain.pem
私钥路径：/etc/letsencrypt/live/openclaw.evanshine.me/privkey.pem
到期时间：2026-07-28
```

手动续期测试：

```bash
certbot renew --dry-run
```

飞书 Webhook 地址改成：

```text
https://openclaw.evanshine.me/webhook/feishu
```

## 12. 后续 AI 接手提示

如果后续交给另一个 AI 接手，可以直接告诉它：

```text
这是 Debian 13 服务器，OpenClawHomework 桥梁服务部署在 /opt/OpenclawHomework。
systemd 服务名是 openclaw-feishu-bridge。
Nginx 对外监听 80，反代到 127.0.0.1:8788。
环境变量在 /etc/openclaw-feishu-bridge.env。
先检查 systemctl status openclaw-feishu-bridge，再 curl /health。
不要把 /etc/openclaw-feishu-bridge.env 里的 token 写进仓库或聊天。
线上自然语言解析已经启用，OPENCLAW_PARSE_ENABLED=true。
如果要让线上主动发飞书结果通知，确认飞书应用有发送消息权限，再配置 FEISHU_APP_ID、FEISHU_APP_SECRET、FEISHU_RESULT_NOTIFY_ENABLED=true。
```
